# Go-Live checklist — standalone copy

The deployable **Go-Live Master Checklist** (UI + scan API + Vercel/Render files) now lives in:

**`Auto-frame/`**

Push **only** that folder as its own GitHub repository (e.g. repo name `Auto-frame`) if you do not want the rest of this monorepo on Git.

See **`Auto-frame/README.md`** for `git init`, `npm run go-live:audit`, and live deploy pointers.
